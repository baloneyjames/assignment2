function addToCart(productName, price) {
  // Code to add the item to the cart
  // For simplicity, we'll just display a message
  var message = productName + " added to cart";
  alert(message);
}

function buyNow(productName, price) {
  // Code to process the purchase
  // For simplicity, we'll just display a thank you message
  var message = "Thank you for buying " + productName;
  alert(message);
}
